<% if(currentUser && currentUser.isDark) { %>
<footer class="footer bg-dark py-3 mt-5">
  <% } else { %>
  <footer class="footer bg-light py-3 mt-5">
    <% } %>
    <div class="container">
      <% if(currentUser && currentUser.isDark) { %> 
      <span class="text-muted footing-copy"
      <% } else { %>
        <span class="text-dark footing-copy"
        <% } %>  
        >&copy; Community Rises 2025
        </span>
        <span style="float: right"
          >Have an idea or encountered difficulties?
          <a href="/suggestions">Go Here!</a> or check the latest
          <a href="/logs">Updates</a></span>
    </div>
  </footer>
</footer>
<script>
  const span = document.querySelector(".footing-copy")
  let date = new Date();
  let year = date.getFullYear();
  span.innerText = `© Community Rises ${year}`
</script>
